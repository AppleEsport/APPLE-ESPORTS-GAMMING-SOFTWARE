# Dependencies
node_modules/
.pnp
.pnp.js

# Environment
.env
.env.local
.env.*.local

# Build output
dist/
build/
*.tgz

# Logs
logs/
*.log
npm-debug.log*

# Runtime
pids/
*.pid
*.seed
*.pid.lock

# Coverage
coverage/
*.lcov

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Docker volumes
pgdata/

# OS
desktop.ini

# .NET Build output
bin/
obj/
.vs/

# Stray debug dumps (docker logs, ad-hoc API responses saved to disk while debugging)
api_logs.txt
pcs.json
*.debug.json

# Dashboard password gate — holds a credential hash, created on the server, never committed
nginx/.htpasswd
docker-compose.override.yml

# Build output, not source
dist/

# Publish output — build artefacts, and the agent exe alone is 156 MB
**/publish/

# Branch installer build output. The PostgreSQL binaries alone are ~120 MB of
# third-party build output, and staging/ is another 300 MB rebuilt from source
# on every run. Neither belongs in git; build-branch-package.ps1 recreates both.
installer/branch/staging/
installer/branch/.cache/
